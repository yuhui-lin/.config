" this already in vim. add two command to behave exactly like this plugin.
Plug 'tpope/vim-markdown', {'for':['markdown']}

" use this only, don't use other fold plugins
Plug 'plasticboy/vim-markdown', {'for':['markdown']}
